<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
   <input class="form-input" type="comment" id="comment" />
   </p>
  <p>
    <button class="btn btn-primary">Add Comment</button>
  </p>
</div>
